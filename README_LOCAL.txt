ELVARA tagline update (LOCAL ONLY)

Adds "Some see limits. We see what's next." as an ELVARA signature statement
in the image-free footer, after Shamika's personal motto and before the
Shamika -> ELVARA -> NIRA brand lineage. Theme-responsive and mobile-friendly.

Extract this ZIP into your existing Shamika-Portfolio repository directory.
It replaces only src/App.jsx and src/ElvaraSignature.css.
Does NOT overwrite NIRA's prompt, chat, background images, API keys or worker.

Run locally:
  npm.cmd run lint
  npm.cmd run build
  npm.cmd run dev

No Git push or Cloudflare deploy necessary while testing.
